import React from 'react'
import styled from 'styled-components';

const fullScreenPopup: React.FC = () => {
    return (
        <FullScreenWrapper>TEUB</FullScreenWrapper>
    )
}

const FullScreenWrapper = styled.div`

    position: absolute;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);

`